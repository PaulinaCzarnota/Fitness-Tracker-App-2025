<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->

    <!-- Step counting (for Android 10 and above) -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <!-- Sensors like heart rate (optional, use only if needed) -->
    <uses-permission android:name="android.permission.BODY_SENSORS" />

    <!-- Required for using internet services (e.g., syncing or fetching data) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Optional for network state monitoring (not used in current app) -->
    <!-- <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> -->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FitnessTrackerApp"
        tools:targetApi="31">

        <!-- Entry point of the app -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.FitnessTrackerApp">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Background step counter service -->
        <service
            android:name="com.example.fitnesstrackerapp.sensors.StepCounterService"
            android:enabled="true"
            android:exported="false" />
    </application>

</manifest>
