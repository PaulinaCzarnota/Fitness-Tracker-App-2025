<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions required by the app -->
    <!-- For step tracking (Android 10+) -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <!-- For sensor access like heart rate (some fitness devices) -->
    <uses-permission android:name="android.permission.BODY_SENSORS" />

    <!-- For syncing data or accessing external APIs -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FitnessTrackerApp"
        tools:targetApi="31">

        <!-- Main launcher activity -->
        <activity
            android:name="com.example.fitnesstrackerapp.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.FitnessTrackerApp">

            <intent-filter>
                <!-- Launcher entry point -->
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- You may declare other activities/fragments here if needed -->

    </application>
</manifest>
