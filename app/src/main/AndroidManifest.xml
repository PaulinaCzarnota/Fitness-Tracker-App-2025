<?xml version="1.0" encoding="utf-8"?>
<!--
    /**
     * Main manifest file for the Fitness Tracker App.
     *
     * Responsibilities:
     * - Declares permissions, application class, activities, services, and receivers.
     * - Ensures all required features and background tasks are registered.
     * - Follows consistent Javadoc-style comment structure.
     */
-->

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        /**
         * Permissions
         *
         * Core Fitness & Health Permissions:
         * - ACTIVITY_RECOGNITION: For step counting and activity detection (Android 10+)
         * - BODY_SENSORS: For accessing health-related sensors and fitness data
         * - HIGH_SAMPLING_RATE_SENSORS: For high-frequency sensor access (Android 12+)
         *
         * Notification & Service Permissions:
         * - POST_NOTIFICATIONS: For sending workout and goal notifications (Android 13+)
         * - FOREGROUND_SERVICE: For running step counter as foreground service
         * - FOREGROUND_SERVICE_HEALTH: Specific type for health-related foreground services
         * - RECEIVE_BOOT_COMPLETED: For re-registering alarms after device reboot
         * - SCHEDULE_EXACT_ALARM: For precise notification and reminder scheduling
         *
         * Network Permissions:
         * - INTERNET: For Firebase authentication and remote APIs
         * - ACCESS_NETWORK_STATE: For checking connectivity status
         *
         * Optional Permissions:
         * - CAMERA: For barcode scanning in food logging (optional feature)
         */
    -->
    <!-- Hardware features -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.sensor.accelerometer"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.sensor.stepcounter"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.sensor.stepdetector"
        android:required="false" />

    <!-- Core fitness and health permissions -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.BODY_SENSORS" />
    <uses-permission android:name="android.permission.HIGH_SAMPLING_RATE_SENSORS"
        tools:ignore="HighSamplingRate" />

    <!-- Notification and service permissions -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_HEALTH" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- Network permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Optional permissions -->
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".FitnessApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FitnessTrackerApp"
        tools:targetApi="33">

        <!--
            /**
             * MainActivity
             *
             * - Entry point for the app.
             * - Hosts the main Compose navigation and UI.
             */
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.FitnessTrackerApp"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            /**
             * StepCounterService (Legacy)
             *
             * - Background service for continuous step counting.
             * - Runs as foreground service for persistent tracking.
             */
        -->
        <service
            android:name=".sensors.StepCounterService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="health" />

        <!--
            /**
             * BatteryOptimizedStepService
             *
             * - Enhanced battery-optimized step counting service.
             * - Includes doze-mode awareness and sensor batching.
             * - Integrates with workouts and goals for comprehensive tracking.
             */
        -->
        <service
            android:name=".sensors.BatteryOptimizedStepService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="health" />

        <!--
            /**
             * NotificationReceiver
             *
             * - Broadcast receiver for daily notifications.
             * - Handles alarm/notification triggers.
             */
        -->
        <receiver
            android:name=".notifications.NotificationReceiver"
            android:enabled="true"
            android:exported="false" />

        <!--
            /**
             * Boot Receiver
             *
             * - Re-registers alarms and notifications after device reboot.
             */
        -->
        <receiver
            android:name=".notifications.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!--
            /**
             * NotificationActionReceiver
             *
             * - Handles notification action button clicks
             * - Processes workout, hydration, and goal reminder actions
             * - Logs user interactions to database
             */
        -->
        <receiver
            android:name=".notifications.NotificationActionReceiver"
            android:enabled="true"
            android:exported="false" />

        <!--
            /**
             * WorkManager initialization provider
             *
             * - Removes default initialization to use custom configuration.
             */
        -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

    </application>

</manifest>
