/**
 * Enum representing different meal types for nutrition tracking.
 *
 * Used to categorize food entries throughout the day for better
 * dietary analysis and meal planning.
 */

package com.example.fitnesstrackerapp.data.entity

/**
 * Meal type enumeration for food entries.
 */
enum class MealType {
    BREAKFAST,
    LUNCH,
    DINNER,
    SNACK,
    PRE_WORKOUT,
    POST_WORKOUT,
    LATE_NIGHT
}
